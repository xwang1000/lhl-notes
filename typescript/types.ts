const x:number = 1
const name:string = 'Nancy'

const playSmash:boolean = true

const cardios:string[] = [
  'bellydance',
  'yoga',
  'walking'
]

// Key value?
const teaDb:{[name:string]:string} = {
  'green tea': 'Unfermented'
}

